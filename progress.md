# Session Progress Log

## Current State

**Last Updated:** 2026-06-16
**Active Feature:** data-013 - 双平台抓取上限自定义（已完成）

## What's Done

- [x] 创建最小开发 harness 和自动一致性校验脚本。
- [x] 审计现有 harness，五个子系统评分为 100/100。
- [x] 确认永恒之塔2台服账号目标列表页并读取公开商品样本。
- [x] 新增只读 DOM 探针，限制为当前页前 16 条，不发起网络请求。
- [x] 新增探针边界校验并接入 `init.sh`。
- [x] 按账号、天族、价格下限 500 的条件，解析区名、种族、价格、职业、装等、战斗力、发布时间和详情链接。
- [x] 新增 `aion2-market-dashboard`，以价格和职业作为查询条件，每次提交都重新读取公开列表页。
- [x] 新增价格摘要、八字段结果表、详情链接和价格升序/降序切换。
- [x] 新增解析单元测试、Vite 构建检查、浏览器交互验证和设计对照 QA。
- [x] 查询时在来源页应用天族、价格和职业条件，并模拟滚动加载每批 16 条的全部结果。
- [x] 后端对商品 ID 去重，串行复用隔离浏览器会话，不使用用户登录态或 Cookie。
- [x] 网站结果改为每页 10 条，支持上一页、下一页、页码跳转，排序后自动回到第 1 页。
- [x] 默认条件实测抓取 87 个源分页、1392 条数据，并呈现为 140 个网站分页。
- [x] 单平台抓取改为最多 100 条，避免密集滚动导致体验下降和触发限流。
- [x] 查询条件新增种族和会员天数；提交查询时仍会重新抓取来源平台。
- [x] 账号数据新增 `source`、`membershipDays`、`sellerRemark` 和 `children`；螃蟹详情页卖家说可解析四连号和小号战力。
- [x] 真实 API 验证 81.9 秒返回 100 条、7 个来源页、来源为“螃蟹”，第一条解析出 `4连号` 子集。
- [x] 新增 7881 来源抓取，使用公开搜索页同源接口 `goods-service-api/api/goods/list`，并复现页面签名头。
- [x] 7881 支持接口侧价格、种族、职业筛选；本地继续按会员天数过滤，每个平台单次最多 100 条。
- [x] 7881 字段映射为统一模型：`战力值` -> 装等，`战力评分K` -> 战斗力，`连体号/标题描述` -> 子集，详情链接为 `https://search.7881.com/{goodsId}.html`。
- [x] 真实聚合验证 85.9 秒返回 200 条：螃蟹 100 条、7881 100 条；总源页 11 页，无来源告警。
- [x] 查询条件新增“查询”按钮，只筛选当前已抓取数据；“重新抓取”按钮才访问螃蟹和 7881。
- [x] 排序入口移入表头，价格、装等、战斗力、会员天数、发布时间均支持升降序切换，且排序不触发重新抓取。
- [x] 移除子集列，改为每行可展开卖家说详情；默认展开排序后前 3 行。
- [x] 浏览器验证首轮数据完成后第 1 页 10 条中有 3 条卖家说展开行，点击装等排序后状态仍为“数据已更新”。
- [x] 发布时间排序夹具验证：API 只调用 1 次，升序最早时间在前，降序最新时间在前。
- [x] 默认种族抓取条件改为“全部”，初始重新抓取包含天族和魔族；螃蟹来源不点击种族筛选，7881 来源不传 groupId。
- [x] 结果分页支持每页 10、50、100 条切换；页数切换只重算本地分页并回到第 1 页，不触发重新抓取。
- [x] 新增 EdgeOne Pages Function：`aion2-market-dashboard/cloud-functions/api/listings.js`，线上映射 `/api/listings`。
- [x] 螃蟹来源从 Playwright/DOM 抓取改为直接调用 `https://api-pc.pxb7.com/api/search/product/v2/selectSearchPageList`，同一轮抓取保持稳定 `device_id`，单来源最多 100 条。
- [x] 7881 继续使用公开搜索页同源接口和签名逻辑；螃蟹与 7881 共用 `scrapeListings`，本地预览和 EdgeOne Function 走同一套纯 HTTP 聚合逻辑。
- [x] 查询条件新增“连体号”：全部、4连号、5连号、6连号、7连号、8连号。
- [x] 商品数据新增 `linkedAccountCount` / `linkedAccountLabel`，并在表格展示连体号列。
- [x] 连体号解析优先读取显式“4连号/5连号/连体号-5连号”；无显式字段时，从“主号 + 小号...”推断，例如“小号158杀+181护+154弓”识别为 4 连号。
- [x] 收紧会员天数解析，只接受明确会员/战令/通行证上下文和会员到期日期，避免把深渊点、战力值、标题数字或天族字段误判为会员天数。
- [x] 真实抓取 200 条后检查会员天数异常值，`membershipDays > 10000` 的结果为 0 条。
- [x] 螃蟹列表 API 原始字段确认 `important` 中存在 `同职业4连号`、`同职业6连号` 等接口标签；归一化时优先合并这些标签参与连体号解析。
- [x] 卖家说兜底解析新增“两个/三个小号 + 角色名 + 146k/185k”类战力描述识别，并保留原有“小号158杀+181护+154弓”识别。
- [x] 抓取上限从固定 100 条改为用户可配置，并拆分为 `螃蟹上限` 和 `7881上限` 两个输入。
- [x] 前端、Vite 本地接口、EdgeOne Function 与抓取核心均已透传 `pxb7Limit` / `source7881Limit`，每个平台按自身上限停止分页。

## What's Next

## Blockers / Risks

- 螃蟹页面 HTML 仍会返回反自动化挑战；当前线上抓取改用其公开 JSON 列表接口，不读取 Cookie、不绕过验证码。详情页“卖家说”暂以列表标题作为展开内容，后续若发现稳定详情 API 再补全。

## Decisions Made

- 优先查询、告警和低风险控制；高风险动作采用申请/审批模式。
- 自动检查声明、注册、路径和组件关联页的一致性，手工检查模型意图与卡片体验。
- 角色交易网站按用户筛选抓取螃蟹和 7881；单平台单次抓取上限由用户分别填写，默认螃蟹 100 条、7881 100 条；默认种族为“全部”，包含天族和魔族；网站分页可在 10、50、100 条之间切换，本地查询、排序和翻页均在前端完成，不重复抓取，不保存历史数据。
- 当前已接入来源值“螃蟹”和“7881”；单个来源失败时保留另一个来源的结果并显示来源告警。
- 详情页“卖家说”以展开行展示；螃蟹卖家说只做 best-effort 增强，单条详情失败时保留列表基础字段，不让整次查询失败。

## Evidence

- 验证命令：`node scripts/validate-wechat-ai-skill.mjs`
- 探针验证：`node scripts/validate-aion2-data-probe.mjs`
- 查询网站验证：`cd aion2-market-dashboard && npm test && npm run build`
- 查询网站真实聚合：`GET /api/listings?minPrice=500&race=天族&profession=弓星` 返回 200 条，螃蟹/7881 各 100 条
- 查询网站浏览器验证：本地查询按钮、表头排序（含发布时间）、默认 3 条展开行均通过
- 查询网站分页验证：默认种族为全部；每页 10/50/100 切换只更新本地分页，未新增 `/api/listings` 调用
- EdgeOne Function 验证：直接调用 `cloud-functions/api/listings.js` 返回 200 条，螃蟹/7881 各 100 条；本地 preview 的 `/api/listings` 返回 `Count=200`、`SourceCount=2`、`Warnings=0`
- 连体号验证：`npm test` 10 项通过；Cloud Function 入口带 `linkedAccount=4连号` 实测返回 47 条，所有结果 `linkedAccountLabel` 均为 `4连号`
- 会员天数修正验证：`npm test` 11 项通过；真实抓取 200 条后 `weirdCount=0`；`npm run build`、`node scripts/validate-aion2-data-probe.mjs` 通过
- 螃蟹连体号验证：`npm test` 14 项通过；真实抓取中螃蟹 100 条有 74 条解析出连体号；Cloud Function `linkedAccount=4连号` 返回 84 条，其中螃蟹 39 条且标签均为 `4连号`
- 双平台上限验证：`npm test` 15 项通过；真实抓取 `pxb7Limit=5&source7881Limit=7` 返回螃蟹 5 条、7881 7 条；Cloud Function `pxb7Limit=3&source7881Limit=4` 返回总计 7 条
- 设计 QA：`aion2-market-dashboard/design-qa.md`
